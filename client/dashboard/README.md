# React + Vite
This is the dashboard which we will use for analyzing the data we recieve from the client while they're on the store_front page.
Right now everything is hard coded, but eventually it will properly show the data we are looking for.