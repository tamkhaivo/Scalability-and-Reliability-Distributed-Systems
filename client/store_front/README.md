# React + Vite
This is the mock store front e-commerce site converted from standalone HTML to React. It uses shadcn for styling the buttons, badges, textareas, inputs, and cards. it also uses lucide for some of the smaller images.

Might be easer to capture 'high-frequency user telemetry data'.